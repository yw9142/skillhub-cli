# Changesets

Create a release note file:

```bash
npm run changeset
```

Version packages locally:

```bash
npm run version-packages
```

Publish through CI (recommended) or manually:

```bash
npm run release
```
